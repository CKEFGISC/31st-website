---
import { ordinal, site } from "~/data/site";
---

<a
    href="/"
    class="navbar-brand"
    aria-label={`${site.shortName} ${site.generation}${ordinal(site.generation)} 首頁`}
>
    <span class="icon">
        <span class="icon-text">
            <img src="/images/chicken_thick.png" alt="" class="mark on-rest" />
            <img src="/images/ckeisc_mouse.png" alt="" class="mark on-hover" />
            <span>&nbsp;CKE</span>
            <span class="on-hover wide"
                >ISC {site.ckeisc}<sup><small>{ordinal(site.ckeisc)}</small></sup></span
            >
            <img src="/images/x.png" alt="" class="mark on-hover" />
            <span>FGISC&nbsp;</span>
            <span class="on-hover">{site.fgisc}<sup><small>{ordinal(site.fgisc)}</small></sup></span>
            <img src="/images/fgisc_star.png" alt="" class="mark on-hover" />
            <span class="on-rest"
                >{site.generation}<sup><small>{ordinal(site.generation)}</small></sup>&nbsp;</span
            >
        </span>
    </span>
</a>

<style>
    /* Bootstrap .navbar-brand metrics */
    .navbar-brand {
        display: inline-block;
        color: #000;
        padding: 0.3125rem 0;
        margin-right: 1rem;
        font-size: 1.25rem;
        white-space: nowrap;
        cursor: pointer;
    }

    .icon {
        display: flex;
        width: fit-content;
        flex-direction: column;
        align-items: center;
        padding: 0.2em 0;
        font-family: var(--font-mono);
        font-size: 1.4rem;
        user-select: none;
    }

    .icon-text {
        display: flex;
        width: fit-content;
        align-items: flex-end;
    }

    .icon-text > * {
        display: inline-block;
        overflow-x: hidden;
        white-space: nowrap;
        transition: 500ms;
    }

    .icon-text img {
        height: auto;
    }

    /* collapsed by default, revealed on hover */
    .on-hover {
        width: 0;
        opacity: 0;
    }
    .on-rest {
        width: 2.25em;
        opacity: 1;
    }
    .mark.on-rest {
        width: 1.5em;
    }

    @media (min-width: 576px) {
        .icon:hover .on-rest {
            width: 0;
            opacity: 0;
        }
        .icon:hover .on-hover {
            width: 2.25em;
            opacity: 1;
        }
        .icon:hover .on-hover.wide {
            width: 4.45em;
        }
        .icon:hover .mark.on-hover {
            width: 1.5em;
        }

        /* ...but a shrunk header stays compact, so the bar does not jump */
        :global(.header-shrink) .icon:hover .on-rest {
            width: 2.25em;
            opacity: 1;
        }
        :global(.header-shrink) .icon:hover .mark.on-rest {
            width: 1.5em;
        }
        :global(.header-shrink) .icon:hover .on-hover {
            width: 0;
            opacity: 0;
        }
    }
</style>
